## 运行时与多平台分发架构

在已有静态编译框架基础上，增加**平台抽象层**和**构建/分发策略**，确保引擎能适配 Windows、macOS、iOS、Android、H5、微信小游戏等主要平台。以下为扩展后的架构：

```
┌──────────────────────────────────────────────────────────────┐
│                      元引擎框架 (编译时)                         │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐ │
│  │ ECS核心    │ │ 资源系统    │ │ 虚拟机(gwg) │ │ 平台抽象层  │ │
│  └────────────┘ └────────────┘ └────────────┘ └────────────┘ │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │              插件组合与构建工具链 (多目标支持)               │ │
│  └──────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
                              │
         (针对目标平台静态链接插件，生成引擎可执行文件)
                              ▼
┌──────────────────────────────────────────────────────────────┐
│                   具体游戏引擎 (多平台构建)                      │
├──────────────┬───────────────┬───────────────┬──────────────┤
│ Windows引擎  │ macOS引擎      │ iOS引擎       │ Android引擎  │
│ (exe)        │ (app/dmg)      │ (ipa)         │ (apk/aab)    │
├──────────────┼───────────────┼───────────────┼──────────────┤
│ H5引擎       │ 微信小游戏引擎  │ ...           │              │
│ (WASM+HTML)  │ (WASM+小游戏适配)│               │              │
└──────────────┴───────────────┴───────────────┴──────────────┘
                              │
          (游戏数据包 + DLC/Mod 跨平台分发)
                              ▼
┌──────────────────────────────────────────────────────────────┐
│                       游戏实例 (运行时)                         │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 游戏数据包（剧本、资源、配置）                          │ │
│  ├────────────────────────────────────────────────────────┤ │
│  │ DLC内容（增量数据）                                    │ │
│  ├────────────────────────────────────────────────────────┤ │
│  │ Mod脚本（gwg代码，需虚拟机执行）                       │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────┐ │
│  │                HMR开发支持（可选，平台限定）              │ │
│  └────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

### 1. 平台抽象层（Platform Abstraction Layer）

**职责**：屏蔽操作系统和运行环境的差异，为上层提供统一接口。包含以下子模块：

#### 1.1 文件系统抽象
- 统一接口：`open(path)`, `read_dir`, `metadata` 等。
- **平台实现**：
  - 桌面：直接映射到操作系统文件系统。
  - 移动端：访问应用沙盒内的指定目录（如 `Documents/`）以及应用资源包（只读）。
  - H5：通过 HTTP 请求读取远程资源，或使用浏览器 IndexedDB 缓存；支持虚拟文件系统挂载。
  - 微信小游戏：通过小游戏 `wx.getFileSystemManager()` 接口访问本地缓存，远程资源需先下载。

#### 1.2 输入抽象
- 统一事件：按键、鼠标/触摸、游戏手柄等。
- **平台实现**：
  - 桌面：使用 `winit` 或 `gilrs`。
  - 移动端：触摸事件、传感器（可选）。
  - H5：DOM 事件。
  - 小游戏：小游戏 API 提供的触摸、键盘事件。

#### 1.3 渲染抽象
- 基于 `wgpu` 或类似图形库，统一 `Surface` 创建和渲染管线。
- **平台支持**：
  - 桌面：原生窗口（`winit` + Vulkan/Metal/DX12）。
  - 移动端：通过 `android_native_app_glue` 或 iOS 的 `UIView` 集成。
  - H5：通过 WebGL2 或 WebGPU（试验性）。
  - 小游戏：小游戏 Canvas 上下文（WebGL1 受限，需适配）。

#### 1.4 音频抽象
- 统一播放控制，支持常见格式（mp3, ogg, wav）。
- **平台实现**：
  - 桌面/移动端：使用 `rodio`（基于 `cpal`）或 `openal`。
  - H5：Web Audio API。
  - 小游戏：小游戏音频接口（`wx.createInnerAudioContext`）。

#### 1.5 时间与线程抽象
- 统一获取系统时间、休眠、线程生成（移动/H5 可能限制多线程）。
- **注意**：H5 环境无法创建原生线程，需使用 `wasm-bindgen` 的 `spawn_local` 模拟异步任务；ECS 系统应设计为单线程调度。

#### 1.6 动态链接与虚拟机支持
- 确保 gwg 虚拟机能在目标平台运行（Wasm 沙箱本身跨平台，但需要宿主支持 WASI 或限制的 API）。
- 对于不支持 WASI 的平台（如微信小游戏），虚拟机需提供纯 WebAssembly 核心，并通过自定义导入函数映射到平台 API。

### 2. 构建与分发流程

#### 2.1 引擎构建（资深玩家）
1. **编写引擎插件**（Rust）并声明依赖。
2. **使用元引擎框架的构建工具**，指定目标平台列表：
   ```bash
   meta-build --targets windows,macos,android --plugins galgame,audio
   ```
3. 构建工具：
   - 为每个目标平台调用相应的编译器（如 `cargo build --target`）。
   - 链接必要的平台 SDK（如 Android NDK, iOS SDK）。
   - 打包资源（如 iOS 的 `Info.plist`，Android 的 `AndroidManifest.xml`）。
   - 生成最终可分发的安装包（exe, app, apk, wasm 等）。

#### 2.2 引擎分发形态
| 平台        | 分发形式                                    | 说明                                                                 |
| ----------- | ------------------------------------------- | -------------------------------------------------------------------- |
| Windows     | `.exe` 安装程序或便携版                     | 包含所有插件和运行时，可独立运行。                                   |
| macOS       | `.app` 或 `.dmg`                            | 需签名以通过 Gatekeeper。                                            |
| iOS         | `.ipa`（通过 TestFlight 或 App Store）       | 需 Apple 开发者证书；内置运行时，无法访问外部文件系统，游戏数据包需通过 iTunes 文件共享或 App 内下载。 |
| Android     | `.apk` 或 `.aab`（Google Play）              | 需签名；支持外部存储读取游戏数据包。                                 |
| H5          | 静态文件（HTML + JS + WASM）                 | 部署在 Web 服务器上，通过浏览器访问。                                |
| 微信小游戏  | 小游戏代码包（包含 WASM 和适配层 JS）        | 需通过微信小游戏审核，遵循小游戏规范。                               |

#### 2.3 游戏数据包（普通玩家创作）
- **数据包结构**：统一为 ZIP 或目录，包含 `game.json` 清单、资源文件夹、剧本文件、DLC 目录等。
- **跨平台兼容**：所有资源路径使用相对路径和正斜杠；图片格式建议使用 PNG/JPEG，音频使用 MP3/AAC（H5 需要兼容格式）。
- **分发方式**：
  - 桌面：玩家手动复制到引擎指定的工作目录。
  - 移动端：通过 iTunes 文件共享、Android 外部存储，或 App 内从服务器下载。
  - H5：数据包可预先打包进 WASM（适用于小型游戏），或通过 HTTP 加载（大型游戏需分块加载）。
  - 小游戏：数据包需随小游戏代码包提交（限制 4MB 内）或通过 CDN 分包下载。

#### 2.4 Mod 与 DLC 的分发
- **DLC**：作为数据包的一部分，由玩家自行添加到游戏项目目录中。
- **Mod**（gwg 脚本）：可独立分发，玩家放置到游戏项目的 `mods/` 目录。平台需允许脚本文件访问：
  - 桌面/移动端：直接读取文件。
  - H5：可通过用户上传或从服务器加载，但需谨慎沙箱安全。
  - 小游戏：Mod 脚本可能需预先打包或通过小游戏开放数据域传输（受限）。

### 3. 各平台特定考虑

#### 3.1 Windows / macOS
- **文件系统**：完全访问，可读写任何用户目录。
- **输入**：键盘、鼠标、手柄。
- **HMR**：支持文件监视，适合开发。
- **编辑器**：可内置完整的编辑器界面。

#### 3.2 iOS
- **沙盒**：游戏数据包应放在 App 的 `Documents` 目录（通过 iTunes 文件共享或 App 内导入）。
- **性能**：需优化 ECS 调度，避免耗电和发热；可能限制后台运行。
- **输入**：触摸、重力感应（可选）。
- **音频**：遵循 iOS 音频会话规则。
- **审核**：需符合 App Store 指南，不能动态下载代码（Mod 脚本需预先打包或通过脚本更新机制但需谨慎）。

#### 3.3 Android
- **存储**：使用 `getExternalFilesDir()` 或 `getFilesDir()`；可读取外部 SD 卡（需权限）。
- **多样性**：处理不同屏幕尺寸、性能分级；支持多种输入（触摸、物理键盘）。
- **后台**：遵循 Activity 生命周期，ECS 世界需能暂停/恢复。
- **权限**：若需访问外部存储需声明权限。

#### 3.4 H5
- **无文件系统**：资源通过 HTTP 加载；可使用 IndexedDB 缓存。
- **初始加载**：WASM 二进制大小需优化（代码分割、压缩）。
- **音频策略**：用户交互后才能播放音频（浏览器限制）。
- **线程**：Web Worker 可用，但 ECS 通常单线程，需考虑 `wasm-bindgen` 的异步支持。
- **HMR**：可通过 WebSocket 推送更新，但需浏览器支持。

#### 3.5 微信小游戏
- **环境限制**：
  - 代码包大小 ≤ 4MB（主包）+ 分包 ≤ 20MB（需按规则）。
  - 不支持完整的 WASI，需使用小游戏提供的 `wx` API。
  - 无法动态加载外部脚本（Mod 需预打包）。
  - 音频、渲染需通过小游戏 API。
- **解决方案**：
  - 将 gwg 虚拟机编译为无外部依赖的 WASM，导出函数供 JS 调用。
  - 游戏数据包可作为分包或通过 CDN 加载（需小游戏缓存管理）。
  - Mod 脚本需在发布前由作者集成，或提供有限的自定义配置选项（非脚本）。
- **性能**：WASM 运行效率较高，但需注意内存占用。

### 4. 运行时适配机制

#### 4.1 启动流程（平台相关）
- 桌面/移动端：引擎启动后，扫描指定目录中的游戏项目清单，用户选择后加载。
- H5：URL 参数指定游戏数据包地址，引擎自动加载并运行。
- 小游戏：启动后根据小游戏配置加载游戏数据。

#### 4.2 资源加载适配
- 平台文件抽象层提供统一的 `AssetIo` trait，不同平台实现不同加载方式。
- 异步加载：在 WASM 环境中使用 `Future` + `wasm-bindgen-futures`，其他平台使用标准线程或异步运行时（如 `tokio` 可选）。

#### 4.3 虚拟机适配
- gwg 脚本所需的环境 API（如打印、ECS 操作）需通过导入函数提供给 WASM 模块。
- 在不同平台上，导入函数的底层实现需调用平台抽象层（如输出到控制台或小游戏日志）。

#### 4.4 编辑器模式的支持
- 仅在桌面平台启用（H5 可考虑轻量版基于浏览器的编辑器，但功能受限）。
- 编辑器与运行时共享大部分代码，通过 feature flag 编译。

### 5. HMR（热模块替换）在多平台下的实现

| 平台        | HMR 支持程度                     | 实现方式                                                                 |
| ----------- | -------------------------------- | ------------------------------------------------------------------------ |
| 桌面        | 完全支持                         | 文件监视器 + 资源重载 + 脚本重载                                         |
| iOS/Android | 开发版支持，生产版禁用           | 通过 USB 调试或网络推送更新（需应用开启调试模式）                        |
| H5          | 开发版支持                       | WebSocket 通知重载，或使用 `window.location.reload()` 简单版             |
| 小游戏      | 不支持（或需小游戏后台更新）     | 只能通过发新版小游戏实现内容更新，无法热更                               |

### 6. 总结

通过引入**平台抽象层**和**多目标构建工具链**，元引擎框架能够生成适配各主要平台的游戏引擎。游戏内容（数据包 + Mod）保持跨平台一致性，开发者只需专注于创作，而底层引擎自动处理平台差异。对于高级玩家，Mod 脚本在虚拟机内运行，确保安全且可移植（除非用到平台特定 API）。最终实现“一次编写引擎，到处运行游戏”的目标。