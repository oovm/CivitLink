## 修正后的架构：元引擎作为编译时框架

根据您的反馈，我将元引擎重新定位为**编译时框架**，最终产出的是一个个独立的游戏引擎（每个包含编辑器与运行时）。以下是调整后的架构设计。

---

### 1. 设计目标回顾
- **普通玩家**：通过配置数据和编写剧本（DLC）就能创作游戏，无需编程。
- **高级玩家**：使用虚拟机脚本（gwg）编写Mod，实现更复杂的自定义逻辑。
- **资深玩家**：基于元引擎框架开发新的引擎插件或全新引擎（Rust代码），静态编译后发布。
- **热更新**：游戏运行时可以动态更新内容（如剧情文本、资源文件），甚至部分脚本逻辑。

### 2. 总体层次结构（静态与动态分离）
```
┌──────────────────────────────────────────────────────────┐
│                    元引擎框架 (编译时)                      │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐           │
│  │ ECS核心    │ │ 资源系统    │ │ 虚拟机(gwg) │           │
│  └────────────┘ └────────────┘ └────────────┘           │
│  ┌────────────────────────────────────────────────────┐ │
│  │                 插件注册与组合机制                    │ │
│  └────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
                              │
          (资深玩家选择插件，编译生成具体引擎)
                              ▼
┌──────────────────────────────────────────────────────────┐
│                   具体游戏引擎 (静态可执行文件)              │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐        │
│  │ Galgame插件 │ │ STG插件      │ │ 基础UI插件   │ ...    │
│  │ (静态链接)   │ │ (静态链接)   │ │ (静态链接)   │        │
│  └─────────────┘ └─────────────┘ └─────────────┘        │
│        内置编辑器（可选） + 运行时（ECS调度器）              │
└──────────────────────────────────────────────────────────┘
                              │
          (普通/高级玩家创作内容，动态加载)
                              ▼
┌──────────────────────────────────────────────────────────┐
│                      游戏实例 (运行时)                      │
│  ┌────────────────────────────────────────────────────┐ │
│  │ 游戏项目（由DLC/Mod构成）                            │ │
│  │  ├─ 引擎配置文件（如 screen_size, default_font）    │ │
│  │  ├─ 资源包（图片、音频、剧本文件等）                  │ │
│  │  ├─ DLC内容（额外章节、角色数据）                    │ │
│  │  └─ Mod脚本（gwg代码，可注册系统/修改组件）           │ │
│  └────────────────────────────────────────────────────┘ │
│                    HMR补丁（实时更新）                     │
└──────────────────────────────────────────────────────────┘
```

### 3. 核心组件职责（重新定义）

#### 3.1 元引擎框架（Meta-Engine Framework）
- **编译时工具链**：提供一组Rust库和宏，用于构建具体引擎。
- **ECS核心**：通用的Entity-Component-System实现，不绑定任何游戏逻辑。
- **资源管理系统**：支持异步加载、热重载，与具体文件格式解耦。
- **虚拟机(gwg)集成**：嵌入轻量级脚本运行时（如Wasmtime或Lua），定义安全的Rust-脚本通信接口。
- **插件注册机制**：允许开发者将多个功能插件（如渲染、音频、输入、特定游戏逻辑）组合成一个完整引擎。插件之间通过ECS世界通信。
- **编辑器生成**：可选地，根据引擎插件暴露的可编辑数据（如剧本结构、实体原型）自动生成编辑器界面（基于egui或类似库）。

#### 3.2 引擎插件（Engine Plugins）
- **每个插件是一个Rust crate**，实现特定功能（例如：`galgame_core`、`bullet_hell`、`dialogue_system`）。
- **插件可定义**：
  - 新的组件类型（Component）
  - 新的系统（System）
  - 资源（Resource，如全局配置）
  - 编辑器面板（可选）
- **插件之间可依赖**：例如`galgame_core`可能依赖`dialogue_system`和`ui_basic`。
- **静态编译**：所有插件在编译时链接到最终引擎可执行文件中。

#### 3.3 具体游戏引擎（Concrete Game Engine）
- 由资深玩家（引擎开发者）使用元引擎框架，选择一组插件，通过简单的`main.rs`组装而成。
- **示例**：一个“Galgame引擎”可能包含`galgame_core + dialogue_system + spine_animation + audio`等插件。
- **引擎包含两部分**：
  - **编辑器模式**：启动时进入编辑界面，允许用户可视化编辑剧情、场景、资源。
  - **运行时模式**：加载游戏项目并运行，执行ECS系统。
- **引擎以独立可执行文件形式发布**，用户下载后即可开始制作对应类型的游戏。

#### 3.4 游戏项目（Game Project）
- 由普通/高级玩家创建，是一个文件夹，包含：
  - **引擎配置文件**（`engine.toml`）：指定使用的引擎名称和版本，以及覆盖默认参数。
  - **资源目录**（`assets/`）：图片、音频、字体等。
  - **剧本文件**（如`.yarn`或自定义格式）：描述剧情、选项、跳转逻辑。
  - **DLC清单**：可选的额外内容包（通常是另一个文件夹，包含增量资源/剧本）。
  - **Mod脚本**（`.gwg`文件）：高级玩家编写的脚本，可注册自定义系统、修改组件等。
- **游戏项目不包含可执行代码**（除Mod脚本外），纯数据和配置。

#### 3.5 DLC（Downloadable Content）
- DLC是游戏项目的扩展包，结构与游戏项目类似，但只包含新增或覆盖的内容。
- **内容类型**：新章节、新角色立绘、新音乐、新剧情分支等。
- **加载方式**：引擎在加载游戏项目时，同时挂载DLC目录，按照优先级合并资源（DLC覆盖原项目）。
- **DLC不包含新逻辑**（除非游戏引擎本身支持通过配置实现逻辑变体），所有行为由引擎插件解释。

#### 3.6 Mod（Modification）
- Mod由高级玩家编写，使用gwg虚拟机脚本，可以：
  - **监听事件**：如“对话开始”、“碰撞发生”。
  - **创建新实体**：例如自定义子弹、敌人。
  - **修改组件数据**：实时影响游戏对象。
  - **注册新系统**：在每帧更新时执行自定义逻辑。
- **Mod脚本运行在沙箱中**，通过虚拟机API安全地访问ECS世界。
- Mod可以与DLC共存：Mod可以读取DLC新增的内容并做出反应。

#### 3.7 HMR补丁（Hot Module Replacement）
- HMR补丁是一组增量更新文件，针对正在运行的游戏实例。
- **支持的类型**：
  - **资源热更**：替换图片、音频文件（引擎自动重新加载）。
  - **数据热更**：修改剧本文件中的某段文本（引擎更新对应对话实体）。
  - **脚本热更**：重新加载修改后的gwg脚本（虚拟机支持代码重载）。
- **HMR机制**：
  1. 引擎的文件监视器检测到变更。
  2. 根据文件类型分发到对应管理器（资源管理器、剧本解析器、虚拟机）。
  3. 管理器应用更新，并通过ECS事件（如`AssetReloaded`、`ScriptReloaded`）通知相关系统。
  4. 系统响应事件，刷新UI或游戏状态。

### 4. 组件关系与数据流（基于修正）

#### 4.1 静态构建流程（资深玩家）
1. 资深玩家选择目标游戏类型，编写或复用现有插件（Rust）。
2. 在`Cargo.toml`中引入元引擎框架和相关插件。
3. 编写`main.rs`，使用元引擎提供的`EngineBuilder`组合插件，生成编辑器+运行时的入口。
   ```rust
   use meta_engine::prelude::*;
   use galgame_plugin::GalgamePlugin;
   use audio_plugin::AudioPlugin;

   fn main() {
       EngineBuilder::new()
           .add_plugin(GalgamePlugin)
           .add_plugin(AudioPlugin)
           .with_editor(true)   // 生成编辑器界面
           .build()
           .run();
   }
   ```
4. 编译生成一个独立的可执行文件（如`galgame_editor.exe`），发布给普通玩家。

#### 4.2 运行时游戏加载流程（普通/高级玩家）
1. 玩家双击引擎可执行文件，进入编辑器模式（或直接运行模式）。
2. 用户选择一个游戏项目文件夹（或新建项目）。
3. 引擎解析`engine.toml`，加载对应插件配置。
4. 资源系统扫描`assets/`，将所有资源加载为ECS中的`Handle<T>`。
5. 剧本文件被解析，生成一系列实体（如对话节点、角色精灵、选项按钮）并赋予初始组件。
6. 如果存在DLC目录，合并其内容（资源覆盖、追加实体）。
7. 如果存在Mod脚本，虚拟机加载并执行`init`函数，Mod可注册自定义系统或监听事件。
8. 进入主循环：ECS系统依次执行（包括引擎内置系统和Mod注册的系统），每帧渲染、处理输入。
9. HMR监听文件变化，随时更新内容。

#### 4.3 动态更新流程（HMR）
- 同上节描述，保持不变。

### 5. 关键技术要点（更新）

#### 5.1 ECS世界的分区
- 每个游戏项目对应一个独立的ECS世界（或通过`World`的命名空间隔离），避免不同项目间的组件冲突。
- 插件系统通过`Schedule`和`SystemSet`组织系统执行顺序，Mod可插入自定义系统到指定阶段。

#### 5.2 虚拟机与ECS的桥接
- gwg虚拟机提供Rust绑定，允许脚本：
  - 查询实体（通过`Query`迭代器，但受限）。
  - 创建/销毁实体。
  - 添加/移除/修改组件。
  - 发送事件。
- 脚本代码被编译为Wasm或解释执行，与Rust核心安全隔离。

#### 5.3 数据驱动设计
- 引擎插件的核心逻辑应尽可能通过数据配置。例如，Galgame插件的对话系统完全由剧本文件驱动，Mod脚本仅用于处理非常规逻辑。
- 剧本文件可定义条件跳转、变量存储，引擎内置解释器执行，减少脚本使用。

#### 5.4 热更新兼容性
- 所有动态数据（资源、剧本、脚本）应具有版本标识，HMR补丁需提供版本匹配检查。
- 组件修改采用事件驱动，确保系统能及时刷新。对于正在进行的动画或对话，可设计“优雅更新”策略（如完成当前对话后再应用文本修改）。

### 6. 总结
- **元引擎框架**：编译时的基石，提供ECS、资源、虚拟机、插件组合机制。
- **具体引擎**：由资深玩家静态构建的独立可执行文件，包含选定插件和可选的编辑器。
- **游戏项目**：纯数据包，由普通玩家通过DLC（数据扩展）和高级玩家通过Mod（脚本扩展）共同创作。
- **HMR**：支持运行时内容迭代，加速创作过程。

这种架构实现了“不同层次玩家各司其职”的目标：
- 普通玩家：只需编辑剧本、配置资源，无需编程。
- 高级玩家：编写Mod脚本，实现自定义机制。
- 资深玩家：开发插件或全新引擎，扩展框架能力。

所有游戏逻辑最终在ECS世界中统一调度，确保性能与灵活性。