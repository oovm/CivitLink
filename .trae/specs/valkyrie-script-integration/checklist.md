- [x] gg-ir 模块定义了完整的 IR 指令集（OpCode），包括常量加载、算术运算、比较、跳转、函数调用、实体/组件操作
- [x] gg-ir 模块定义了 IrModule 和 IrFunction 结构，能表示一个完整的脚本模块
- [x] gg-vm 模块实现了字节码虚拟机，能执行基本指令（算术、比较、跳转、函数调用）
- [x] gg-vm 模块实现了宿主接口（Host），VM 能通过宿主接口调用引擎 Rust API
- [x] gg-vm 模块实现了实体/组件操作指令，脚本能创建实体和操作组件
- [x] gg-script 模块实现了脚本编译器，能将 Valkyrie 风格的脚本源码编译为 IR（使用自定义的 SimpleLexer + SimpleParser）
- [x] gg-script 模块实现了词法分析和递归下降解析器，直接生成 IR 模块
- [x] gg-runtime-core 集成了脚本执行环境，Runtime 包含 ScriptEngine
- [x] 游戏循环中脚本的 init/update 函数能被正确调用
- [x] examples/basic 使用 Valkyrie 脚本编写游戏逻辑，不再直接用 Rust
- [x] 示例能成功编译运行，脚本能创建实体、添加组件、执行系统逻辑
- [x] 所有新增的 public 结构体、枚举、方法、字段都有文档注释
